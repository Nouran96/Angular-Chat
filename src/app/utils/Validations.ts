import { ValidationMsgs } from '../models/ValidationMsgs';

export const ValidationMessages: ValidationMsgs = {
  required: 'Field is required',
  email: 'Invalid email address',
  passwordMismatch: "Passwords don't match",
};
