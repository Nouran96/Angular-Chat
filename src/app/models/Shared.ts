export interface SharedState {
  openSnackbar: boolean;
  message: string;
  className?: string;
}

export interface Snackbar {
  openSnackbar: boolean;
  message: string;
  className?: string;
}
